<UserControl x:Class="ContainerLoadPlanner.Views.ContainerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ContainerLoadPlanner.Views"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <md:Card Margin="10">
            <Grid Height="auto"
                  >

                <StackPanel>
                <DataGrid ItemsSource="{Binding Items}" AutoGenerateColumns="True">

                    </DataGrid>
                    <DataGrid
      ItemsSource="{Binding Summaries}" AutoGenerateColumns="False"
      ></DataGrid>
                    <DataGrid x:Name="ContainerSummary"
            
            
            ItemsSource="{DynamicResource ContainerSummary}"></DataGrid>
                </StackPanel>
              
            </Grid>
        </md:Card>
        
    </Grid>
</UserControl>
